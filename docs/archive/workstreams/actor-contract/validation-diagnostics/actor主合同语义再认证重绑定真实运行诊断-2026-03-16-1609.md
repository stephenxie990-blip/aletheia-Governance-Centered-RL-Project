# actor 主合同语义再认证重绑定真实运行诊断

- 时间：2026-03-16 16:09
- run：`outputs/exp_seed42_v179_h15_actor_contract_semantic_recert_rebind_2500_20260316_01`
- 阶段：第二阶段实现后的真实 pure-imag 验收
- 目标：验证 `semantic-certified rebind` 在真实训练中，是否能把“仍在 certified corridor 内的语义恶化”真正接入 actor 主合同，并改善 `2000+` late hold

## 真实结果

### eval 历史

- `250 = 49.0`
- `500 = 131.8`
- `750 = 68.2`
- `1000 = 153.8`
- `1250 = 135.4`
- `1500 = 285.8`
- `1750 = 105.0`
- `2000 = 57.2`
- `2250 = 56.4`
- `2500 = 59.4`

### 关键结论

1. 这条 run 证明第二阶段实现不是“只在测试里成立”：
   - `rebind_alpha` 在真实 run 中持续非零；
   - semantic mismatch / inflation pressure 会真实进入 actor 主链。
2. 但这条 run 也明确失败于 late hold：
   - `best_eval = 285.8 @ step 1500`
   - `2500 current = 59.4`
   - `2000/2250/2500` 全部没有拉回。
3. 对比 `v178`：
   - `v179` 在 `500/750/1000/1500` 明显更强；
   - 但在 `2000/2250/2500` 明显更差于 `v178` 的 `116.8/128.6/171.2`。
4. 因此这次修复的真实效果是：
   - 它改善了“前中段起势与中段冲高”；
   - 但没有解决“冲高后在 corridor 内的长期语义保持”。

## 病理表现

### 1. 失败时并没有掉出 certified corridor

从 `750` 开始，真实侧几乎始终处于 corridor 内：

- `750`: `occupancy=0.9824`, `registry_kl=0.00714`
- `1000`: `occupancy=1.0`, `registry_kl=0.01761`
- `1250`: `occupancy=1.0`, `registry_kl=0.00231`
- `1500`: `occupancy=0.9993`, `registry_kl=0.00459`
- `1750`: `occupancy=1.0`, `registry_kl=0.00106`
- `2000`: `occupancy=1.0`, `registry_kl=0.00658`
- `2250`: `occupancy=1.0`, `registry_kl=0.00296`
- `2500`: `occupancy=1.0`, `registry_kl=0.00160`

这把当前主病灶进一步钉死为：

- 不是 corridor exit；
- 不是 registry coverage 不足；
- 而是 corridor 内部的任务语义与行为稳定性一起退化。

### 2. 行为层在 corridor 内重新变得高频抖动

真实行为量显示：

- `1500` 高分窗口：
  - `action_switch_rate = 0.7612`
  - `action_oscillation_rate = 0.5229`
- `1750` 回落后：
  - `action_switch_rate = 0.8981`
  - `action_oscillation_rate = 0.7942`
- `2000`：
  - `action_switch_rate = 0.8363`
  - `action_oscillation_rate = 0.6884`
- `2500`：
  - `action_switch_rate = 0.8322`
  - `action_oscillation_rate = 0.6934`

这说明：

- 策略不是“迷路离开 corridor”；
- 而是在 corridor 内又重新变成高频切换、高频往返的行为风格。

### 3. actor 主合同知道要修，但没有拿到主导权

尾段关键指标如下：

- `2100`：
  - `semantic_mismatch = 0.0691`
  - `inflation_pressure = 0.1716`
  - `rebind_pressure = 0.0493`
  - `rebind_alpha = 0.00567`
  - `analytic_scale = 0.99802`
  - `weighted_actor_target = 0.4022`
  - `adv_mean = -1.2993`
- `2200`：
  - `semantic_mismatch = 0.0380`
  - `inflation_pressure = 0.1479`
  - `rebind_alpha = 0.00474`
  - `analytic_scale = 0.99834`
  - `weighted_actor_target = 0.3657`
  - `adv_mean = -1.3025`
- `2300`：
  - `rebind_alpha = 0.00341`
  - `analytic_scale = 0.99881`
  - `weighted_actor_target = 0.3684`
  - `adv_mean = -0.6586`
- `2400`：
  - `semantic_mismatch = 0.0839`
  - `rebind_alpha = 0.00158`
  - `analytic_scale = 0.99945`
  - `weighted_actor_target = 0.5865`
  - `adv_mean = 1.6768`
- `2500`：
  - `semantic_mismatch = 0.1102`
  - `inflation_pressure = 0.0970`
  - `rebind_alpha = 0.00455`
  - `analytic_scale = 0.99841`
  - `weighted_actor_target = 0.3753`
  - `adv_mean = -0.6180`
  - `online_adv_mean = 0.3217`
  - `real_mc_gap_abs = 5.8348`

这组数据说明：

1. `semantic-certified rebind` 并没有失效；
2. 它在 late-stage 也持续被触发；
3. 但其量级只在 `0.001 ~ 0.006`，`analytic_scale` 仍几乎等于 `1.0`；
4. 因而它更像“背景修正”，不是“主合同接管”。

## 诊断判断

### 已经排除的病因

1. 不是 registry corridor 不存在：
   - `registry_size` 已增到 `4`；
   - `registry KL` 长期很低。
2. 不是 late failure 主要来自 corridor 外漂移：
   - occupancy 与 persistence 全程近满。
3. 不是第二阶段 rebind 根本没进入真实训练：
   - `rebind_alpha` 在真实 run 中持续非零。

### 当前最可信的病灶

当前主病灶已经收紧为：

> `semantic-certified rebind` 解决了“是否允许接管”的结构盲点，但还没有解决“谁真正支配 actor 主合同”的权力问题。

更直白地说：

- actor 已经能看见 semantic risk；
- 也已经能生成 clean-contract rebind；
- 但旧的 `weighted_actor_target` 仍然长期占主导；
- 因此策略会在 certified corridor 内继续被旧 target 拉着走，最后退化成低效振荡。

### 根本原因

当前根本原因不是“检测不到”，而是：

1. `semantic-certified mismatch` 已经进入观测面；
2. 但 actor 主合同的 dominant term 仍然是旧的 internal target；
3. rebind 目前只是轻量叠加，而不是主合同重写；
4. 因此系统会出现：
   - 仍在 certified corridor 内；
   - policy KL 看起来健康；
   - 但真实行为与任务语义一起恶化。

## 对下一步的约束

这轮 run 结束后，下一步不应再回到：

- `controller-first`
- `critic-only`
- `corridor exit` 路线

因为本轮真实证据已经说明：

- 主病灶不在 corridor 外；
- 主病灶在 corridor 内；
- 问题是 actor 主合同对语义再认证的支配力度不足。

## 当前结论

1. 第二阶段实现方向正确，真实 run 已证明其确实进入主链。
2. 但当前版本只修到“rebind exists”，没有修到“rebind dominates”。
3. 最新病理正式收敛为：
   - `certified corridor` 仍健康；
   - `semantic risk` 已被检测；
   - 但 actor 主推进器没有被真正重绑定；
   - 系统于是沿着 corridor 内部的错误行为模式继续滑落。
