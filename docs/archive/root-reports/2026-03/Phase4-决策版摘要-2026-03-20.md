# Phase 4 决策版摘要

日期：2026-03-20

## 1. 当前只保留这一个判断

> 现在不过线的主因，不应再回打到 `WM core`；
> 当前真正影响下一刀的，只剩 `post-transition certified capture source` 是否真的把历史 authority 留住。

`v242` 完整跑完后，这个判断比之前更硬了。

---

## 2. 真正影响下一刀的 run

| run | 为什么保留 | 只记这一句话 |
| --- | --- | --- |
| `v232` | 旧 `5 ep` 主实现基线 | 证明旧 bridge 线曾经能做到 `2250=500 / 2500=500`，但不能与新 `15 ep` 直接横比。 |
| `v237 phase420` | 旧 `5 ep` 次基线 | 证明 `handoff + certified floor recoupling` 也能打出 `2500=500`，是旧线的第二个晚窗高点。 |
| `v240` | 新 `15 ep` 第一条关键负样本 | 证明 availability/open gates 不是问题，真正问题是 durability 不够。 |
| `v241` | `post-transition certified retention floor` 检验样本 | 指标接上了，但行为与 `v240` 完全相同，说明 floor 只是复述当前退化 authority。 |
| `v242` | `capture-source replacement` 完整样本 | 已完整跑完，但行为仍与 `v240/v241` 同型，说明当前实现还没有让历史 authority 真正赢过当前 authority。 |

补充但不作为当前主判决的 run：

- `v229`
  中段参考，不允许未来为了守晚窗再把 `1500/1750/2000` 做塌。
- `v231`
  失败护栏，提醒我们“hold 更强”不等于“终局更稳”。

---

## 3. 一张表看懂 `v240 / v241 / v242`

| run | `1250` | `1500` | `1750` | `2000` | `2250` | `2500` | 裁决 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| `v240` | `203.9` | `92.2` | `51.9` | `264.3` | `25.0` | `22.5` | gate 开着，但 authority durability 不够。 |
| `v241` | `203.9` | `92.2` | `51.9` | `264.3` | `25.0` | `22.5` | floor 已接上，但没有净效果。 |
| `v242` | `203.9` | `92.2` | `51.9` | `264.3` | `25.0` | `22.5` | capture source 当前实现仍无净效果。 |

最关键的新事实是 `v242` 的结构量：

| step | current authority | historical authority | capture source |
| --- | ---: | ---: | ---: |
| `1500` | `0.1798` | `0.1582` | `0.1798` |
| `2000` | `0.0762` | `0.0759` | `0.0762` |
| `2500` | `0.0531` | `0.0504` | `0.0531` |

这说明：

> 历史 authority 已经被接进来了，但在真实运行中从未真正压过当前 authority；
> 所以 `capture source` 仍然是 contemporaneous mirror，而不是 durable capture。

---

## 4. 下一刀只许这样下

唯一允许继续推进的层：

- [`_compute_post_transition_certified_retention_floor_contract(...)`](/Users/zhangsan/Desktop/缸中之脑v5.6/aletheia/aletheia_train.py) 内部的 `post_transition_certified_capture_source`

明确冻结：

- `WM core`
- imagined geometry
- corridor
- entry / trigger / threshold / ramp
- hold path 本体
- actor
- controller

---

## 5. 后续只跑这 2-3 个 A/B

1. `A/B-1`
   `v242` 精确基线 vs `historical-authority dominance` 版本。
   目标是第一次在真实关键点上打出 `capture_source > current_authority`。

2. `A/B-2`
   只有 `A/B-1` 结构通过才允许启动。
   对 `A/B-1` 胜者做同口径精确 rerun，验证不是一次性噪声。

3. `A/B-3`
   只有当 `capture source` 已经打穿、但行为仍贴着 `v242` 时，才允许进入 `floor-to-gate coupling` 的下一层 A/B。

---

## 6. 决策结论

当前最重要的不是“再多试几个想法”，而是：

> 先把 `capture source` 这一层做成一次真实、可复现、可观测的结构胜利。

在这之前，不要重开别的层，也不要把当前失败重新解释成世界模型精度问题。
